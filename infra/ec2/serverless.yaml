service: visualization-ec2

frameworkVersion: '3'
configValidationMode: error

provider:
  name: aws
  region: eu-central-1
  stage: dev

custom:
  postgresPassword: ${env:POSTGRES_PASSWORD}

resources:
  - ${file(./ec2.yaml)}
  - Parameters:
      DbPassword: ${self:custom.postgresPassword}